pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo step build'
            }
        }
        stage('test') {
            steps {
                sh 'echo step test'
            }
        }
        stage('deploy') {
            steps {
                sh 'echo step deploy'
            }
        }
    }
}